{
  "name": "lifebook-ingest",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild dist/index.js --platform=node --format=cjs --bundle --outfile=dist/bundle.js --target=node20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.657.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "esbuild": "^0.25.0",
    "@types/aws-lambda": "^8.10.131"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
