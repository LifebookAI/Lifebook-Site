# ─── App basics ──────────────────────────────────────────────────────────────
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=Lifebook.AI
# Local dev URL; prod value comes from your hosting env
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─── Upload / storage (no secrets) ───────────────────────────────────────────
# Your S3 bucket for source uploads (placeholder)
UPLOAD_BUCKET=lifebookai-uploads
# The AWS region your bucket/Lambda are in
AWS_REGION=us-east-1
# CloudFront (or files) domain used to view downloads
FILES_CDN_DOMAIN=files.uselifebook.ai

# ─── Presign service (Lambda behind API Gateway) ─────────────────────────────
# Base URL for the presign endpoint (no trailing slash)
PRESIGN_BASE_URL=https://YOUR_API_ID.execute-api.us-east-1.amazonaws.com/prod
# HMAC secret (hex) used by your Next.js API route to sign requests to Lambda.
# DO NOT commit a real value; put the real hex secret in .env.local only.
PRESIGN_HMAC_SECRET_HEX=REPLACE_WITH_64_CHAR_HEX

# ─── Client limits (safe to expose) ──────────────────────────────────────────
# Max upload size enforced in UI (in MB). Real caps are also enforced server-side.
NEXT_PUBLIC_MAX_UPLOAD_MB=8192
